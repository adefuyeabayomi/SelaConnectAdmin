import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  sInfoContainer: {
    padding: 10,
    borderRadius: 16,
    borderWidth: 1.5,
    borderColor: "#CCE2FF",
  },
});

export default styles;
